#!/bin/sh

#-----------------------------------------------------------
# NAME         :  bluetooth_check
# AUTHOR       :  Adrien CARBONARO
# UPDATE       :  20/01/2019
# DESCRIPTION  :  Check if bluetooth is connected to device
# PARAMETERS   :  none
#-----------------------------------------------------------

if [ "$(pgrep redshift)" ]; then
    echo "JBL FLip 3"
else
    echo "..."
fi
